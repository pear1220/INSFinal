<?xml version="1.0" encoding="UTF-8"?>

<!-- ==== #26. mapper 기본설정 ==== -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<!-- ==== #27. 루트 엘리먼트 & 네임스페이스 설정(프로젝트 전체내에서 유일해야 한다.) ==== -->
<mapper namespace="jihye">

   <!--  ====== #56. 글쓰기(파일첨부가 없는 글쓰기) -->
	<insert id="add" parameterType="com.spring.finalins.qna.model.QnaVO" >
	    insert into ins_QnA(seq, userid, name, subject, content, pw, readCount, regDate, status)
        values(boardSeq.nextval, #{userid},#{name} , #{subject}, #{content},#{pw} , default, default, default)
	</insert>

</mapper>